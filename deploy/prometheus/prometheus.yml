global:
  scrape_interval: 15s

scrape_configs:
  - job_name: django
    metrics_path: /metrics
    static_configs:
      - targets:
          - webapp:8000

  - job_name: nginx
    metrics_path: /metrics
    static_configs:
      - targets:
          - nginx-exporter:9113
